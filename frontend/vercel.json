{
  "version": 2,
  "builds": [
    {
      "src": "frontend/package.json",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "/frontend/$1" }
  ],
  "buildCommand": "cd frontend && npm install --legacy-peer-deps && npm install -g vite typescript && npm run build",
  "outputDirectory": "frontend/dist"
}
